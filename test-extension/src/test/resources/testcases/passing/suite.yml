---
name: passing suite
options:
  - !creek/kafka-options@1
    notes: test using a non-default kafka server
    kafkaDockerImage: "confluentinc/cp-kafka:8.1.0"
services:
  - native-service
tests:
  - name: test 1
    inputs:
      - input
    expectations:
      - output
